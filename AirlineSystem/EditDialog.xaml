<Window
    x:Class="AirlineSystem.EditDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Edit Item"
    Width="400"
    Height="400"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Dynamic content  -->
        <StackPanel
            x:Name="ContentPanel"
            Grid.Row="0"
            Margin="0,0,0,10" />

        <!--  Save/Cancel  -->
        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="80"
                Margin="5"
                Click="Save_Click"
                Content="💾 Save" />
            <Button
                Width="80"
                Margin="5"
                Click="Cancel_Click"
                Content="❌ Cancel" />
        </StackPanel>
    </Grid>
</Window>
